source 'https://cdn.cocoapods.org/'
use_frameworks! :linkage => :static
inhibit_all_warnings!

platform :ios, '13.0'

target 'TryLink' do
    pod 'RxSwift'

    target 'TryLinkTests' do
        inherit! :search_paths
    end
end